# 屏幕伙伴更新日志

## 未发布

### 远程数据来源对齐

- 修复远程模式下系统状态提示读取服务器本机 CPU/内存/电量的问题：客户端随帧上报的 `system_stats` 此前只入库不使用，导致模型会基于插件主机的负载给出"当前设备电量偏低""系统负载较高"等针对错误机器的建议；现在远程分支只读客户端统计，没有可信数据时不产生提示，也不回落本机采样。
- 修复远程模式下活动窗口标题读取服务器窗口的问题：`pygetwindow` 在远程模式会返回与用户无关的结果，污染活动轨迹、工作段聚合、日记、场景判断与 `screen_usage_context` 工具；现在远程分支改用最近一次完整帧的窗口标题，`region` 固定为空（远程裁剪由客户端完成）。
- 新增接收器只读探测 `latest_window_title` 与 `latest_system_stats`，只返回最近一次完整帧的数据，不发送网络请求、不触发采集。
- 独立活动轨迹采集在远程模式下按帧新鲜度判断：过期帧不再延长上一条活动（否则一次识屏会被记成持续数小时的同一窗口），帧过期时按"查不到窗口"处理并停止计时。
- 健康检查在远程模式下改用"随识屏更新、时间分辨率低于本机"的说明，帧过期时标记为警告，不再沿用本地"采样间隔"的说法。
- 浏览器历史旁证在远程模式下返回空候选，不再读取服务器浏览器数据库冒充用户行为。
- 本地输入统计在远程模式下明确停用并说明原因（`remote_unsupported`），不监听服务器键鼠；麦克风音量触发识屏在远程模式不启动本机监听任务，也不再提示服务器缺失 `pyaudio`。
- `/kpr` 在远程模式下复用客户端最近上传的录屏，并如实提示"无法命令客户端立刻补录"，不再提示"正在录制"或用服务端 `ffmpeg` 录服务器桌面。
- 远程主动观察显式要求新帧（`force_fresh_capture=True`），不再依赖"客户端恰好会重拍"的实现细节。
- 自动观察的高负载判定改读客户端上报的系统统计，不再用服务器负载决定是否触发对用户屏幕的识屏。
- 窗口列表枚举在远程模式下返回空列表，不再把服务器桌面窗口当作"用户新窗口"写入活动轨迹或触发窗口陪伴会话。
- 客户端录屏的窗口标题改为取**录制开始前**的活动窗口，与本地录屏语义一致；录制期间切换窗口时不再把之后切到的窗口配给这段视频。

### 远程活动窗口裁剪

- 修复「只截取活动窗口」在远程模式下完全不生效的问题：该开关此前只有本地截图路径使用，远程客户端始终上传整屏。
- 远程模式下由客户端执行裁剪，并通过能力协商（`capture_active_window`）声明支持；声明能力不等于每次都能裁剪，取不到窗口矩形时回退整屏。
- 服务端在每次按需请求中下发范围要求，并在握手时下发会话级默认（显式 `--push` 周期推送使用这一层）。
- 新增客户端参数 `--active-window-only` 与 `--no-active-window`，可覆盖插件端配置。
- 窗口标题与矩形改为**一次查询**同时取回，避免切换窗口时把两幅画面配到一起；裁剪前校验矩形稳定性，变化时最多补拍一次。
- 客户端随每帧上报 `capture_scope`（`window` / `fullscreen`），服务端如实入库；插件端承诺裁剪但实际回退整屏时记录一次可排查的提示，不伪装成裁剪成功。
- Windows 客户端启动时启用 per-monitor DPI 感知，避免系统缩放导致裁剪区域偏移；最小化窗口（坐标约 -32000）与过小矩形会被识别为不可用并回退。
- 多显示器负坐标按合法值处理，不再被误判为无效矩形。
- 跨平台几何来源：Windows 用 `GetForegroundWindow` 加 DWM 可见边界，macOS 用 AppleScript，Linux 用 `xdotool`。

### 远程按需识屏

- 远程模式改为默认按需截图：客户端连接后只保活，只有插件请求时才采集一张新图并回传，空闲期间不再产生图片流量。
- 新增协议版本与能力协商（`protocol_version: 2`、`request_screenshot`）；服务端用 `request_id` 与连接绑定请求，只把对应帧交给本次识屏。
- 识屏入口落实强制重拍：`/kp`、自然语言识屏与屏幕工具在远程图像模式下请求新帧，不再直接读取上一次上传的缓存；失败时返回明确原因，不用旧图冒充成功。
- 图片与窗口元数据按连接暂存、整帧完成后一次性提交，避免“新标题 + 旧图片”的配对错位与多客户端交错混用。
- 新增单个在途截图限制：并发第二个请求，或显式录屏/周期推送占用期间，明确返回 busy，不排队、不返回旧缓存。
- 处理超时、取消、断线、迟到响应与无效 JPEG：迟到帧不完成后续请求，也不覆盖有效缓存。
- 远程录屏移除把任意缓存截图标记为“现在”的实时锚点，窗口标题只取视频自身元数据。

### 远程客户端

- 默认不再执行周期截图循环、窗口轮询与截图预热；新增 `--push` 显式启用周期上传。
- 客户端改为单一接收循环分发截图请求、ACK 与错误，消除多处并发 `recv()` 竞争；录屏上传接入同一交换函数。
- 窗口标题查询改用短预算（单次最多 0.5 秒、单轮合计最多 1 秒），窗口切换时最多补拍一次，无法稳定配对时返回空标题。
- 按需路径不再采集可选系统统计，避免 CPU/电池采样拖慢结果；`--push` 的统计改为非阻塞并允许失败降级。
- 保活改用 WebSocket 原生 ping/pong；`--heartbeat 0` 表示关闭主动保活。
- 旧服务端默认明确提示升级并以非零状态退出；`--push` 与 `--video-only` 仍可在旧协议下使用。
- 参数校验加固：`--quality`、`--interval`、`--heartbeat`、`--video-duration` 拒绝非法数值，`--binary` 与 `--json` 互斥。

### 兼容性

- 旧插件仍会收到并接受新客户端的显式周期推送；旧客户端在新插件下仍可按缓存路径工作，但强制重拍会提示升级客户端。
- 服务端缓存年龄改用单调时钟，不受本机校时或墙钟跳变影响。

## 3.3.1 (2026-08-15)

### 远程识屏

- 远程客户端支持直接上传二进制 JPEG，减少 Base64 编解码与传输开销，并为截图接收增加确认响应。
- 新增远程短录屏采集与分块上传；服务端会校验上传大小、分块完整性和时效，远程录屏模式改为读取客户端上传的视频。
- 远程客户端可同步输入统计和麦克风音量，并在截图、录屏或可选依赖不可用时返回更明确的错误。

### 模型与权限

- 配置的独立视觉 Provider 可直接用于画面识别，再由当前会话模型组织最终回复；主视觉 Provider 失败时可回退到备用 Provider。
- 修复屏幕工具调用时真实消息事件丢失的问题，使管理员私聊权限校验兼容不同 AstrBot 工具上下文。

### 稳定性与配置

- 新增“发送开始/结束提醒”独立开关，关闭提醒不再影响固定文案与 LLM 文案的其他配置。
- 配置保存增加落盘校验，远程接收器与识别模式会跟随运行时配置重新同步。
- 自动识屏任务异常退出后会进行有界恢复，避免单次异常令任务永久停止或进入反复重启。

## 3.3.0 (2026-07-23)

- 新增工作协同扩展能力：其他插件可先探测能力，再异步读取当前窗口、活动状态和五分钟内的最近识屏摘要。
- 工作协同上下文只复用现有缓存，不触发新的截图或视觉模型调用，也不会向调用方返回图片、帧列表和对话回复预览。
- 工作协同上下文遵循窗口标题脱敏配置，并限制所有外发字段长度；读取异常时安全降级为空上下文。
- 新增 `shared_work` 共同活动类型，结束后按“工作 / 办公”记录实际协同时长。

## 3.2.3 (2026-07-20)

- 新增共同活动生命周期接口，通话和一起看视频可作为明确来源的活动进入使用轨迹，并在结束时记录实际持续时间。
- 外部共同活动使用独立运行态，不覆盖屏幕观察正在记录的窗口活动，避免联动后打断原有工作/娱乐统计。

## 3.2.2 (2026-07-10)

### 安全

- 加固远程识屏接收链路：限制消息与截图大小、校验 JPEG 和 Base64 数据、验证端口范围，并使用恒定时间比较认证令牌。

### 优化

- 学习数据、观察记录、长期记忆、活动历史、日记状态和输入统计等 JSON 状态文件改为原子写入，降低进程中断时文件损坏的风险。
- 修复浏览、办公、视频、阅读和音乐场景中的中文应用分类关键词乱码，并保留对历史问号占位脏数据的清理能力。

## 3.2.1 (2026-07-10)

### 修复

- 修复 `/kp`、`/kpr` 已成功执行后，回复模型仍可能把命令当作普通聊天文本并询问命令含义的问题。
- 兼容 AstrBot 不同版本的对话历史消息结构，并修复上下文兜底调用不存在方法导致的上下文丢失。

### 安全

- 收紧 LLM 屏幕工具权限：`screen_peek` 与 `screen_usage_context` 只能在屏幕伙伴已启用、管理员私聊授权的场景执行；群聊或非私聊上下文即使模型发起工具调用，也会在执行入口直接拒绝。
- 自然语言识屏求助不再接受群聊触发，避免群成员通过普通聊天让 Bot 查看部署电脑屏幕或近期电脑使用情况。

## 3.2.0 (2026-06-24)

### 新增

- 新增远程识屏模式：插件可通过内置 WebSocket 接收远程客户端推送的桌面截图，适配云端 AstrBot 加本地桌面客户端的部署方式。
- 新增 `remote_client.py`，可在本地图形桌面上抓取截图并推送到远程插件实例。

### 修复

- 修复旧版视频兼容直发可能错误构造 `image_url`，导致部分视觉 provider 返回 `Invalid base64 image_url` 的问题。
- 修复 WebUI 内展示的实例版本长期停留在旧值的问题，统一同步到插件版本号。

### 优化

- 远程模式新增截图时效判断，客户端断开或长时间未推送时会明确提示远程截图已过期。
- 远程模式环境检查会额外校验 `websockets` 依赖和接收服务启动状态，减少“配置看起来正常但运行时才失败”的情况。
- 远程模式下统一收敛为截图链路，不再误走本地录屏；`/kpr` 会直接提示当前远程模式不支持录屏。

## 3.1.3 (2026-06-12)

### 新增

- 录屏识别新增 macOS 支持，macOS 下会通过 ffmpeg `avfoundation` 自动寻找 `Capture screen` 设备进行桌面录制。

### 优化

- `/kpi ffmpeg` 在非 Windows 系统下会将二进制保存为 `ffmpeg`，不再统一保存为 `ffmpeg.exe`。
- 录屏环境检查改为区分 Windows、macOS 和其它平台，给出更贴近当前系统的 ffmpeg 安装提示。

## 3.1.2 (2026-06-12)

### 修复

- 修复 Windows 上启用本地输入统计后，`pynput` 全局鼠标钩子可能导致鼠标周期性卡顿的问题。

### 优化

- 本地输入统计默认仅保留键盘监听，避免鼠标点击、滚轮和移动监听影响光标流畅度。
- 降低自动识屏等待期间的窗口变化检测频率，减少频繁调用系统窗口 API 带来的性能抖动。

## 3.1.1 (2026-06-06)

### 修复

- 修复 AstrBot 插件拓展页内最新截图与录屏预览无法正确显示的问题。

### 文档

- 在 README 显眼位置推荐搭配使用“我会永远陪着你”插件。

## 3.1.0 (2026-05-23)

### 新增

- WebUI 迁移至 AstrBot 插件拓展页面，可在插件详情中直接打开“屏幕伴侣”
- 本地输入统计新增历史总计，并改为长期保留轻量统计数据

### 修复

- 修复日记换行在 WebUI 中显示成大量 br 文本的问题

## 3.0.0 (2026-04-27)

### 调整

- 将“陪伴模式”和“偷看模式”收敛为单一的“互动风格”选项，避免配置上出现同时开启的歧义
- 配置说明整体改写得更简洁，并为常见选填项补上“可留空 / 不填会怎样”的明确提示

### 优化

- 主动陪伴链路补上短期对话记忆，减少几分钟后像忘记上文一样重新起话头
- 收紧识屏回复中的客观播报式开场，减少“你在……啊 / 现在在……”这类旁白句式
- 视觉识别配置改为优先选择 AstrBot provider，并保留旧独立 API 作为兼容兜底

## 2.9.9 (2026-04-19)

### 新增

- 新增 AstrBot LLM 工具 `screen_peek` 与 `screen_usage_context`，让 bot 可以自行决定何时窥屏或查阅近期电脑使用情况

### 优化

- 电脑使用情况摘要新增“最近时间分布”和“最近常用应用”聚合，减少信息零散感
- 统一工具输出和主识屏链路的使用情况摘要口径，降低模板化和重复描述

## 2.9.8 (2026-04-10)

### 优化

- 调整内置识屏技能的素材获取逻辑：bot 自行调用识屏时会优先立即抓取最新截图或重新录制当前录屏片段，减少误用旧资源的情况。
- 将识屏回复决策改为“意图优先”：先提炼用户这轮要确认的目标，再判断当前画面是否真的覆盖该目标，最后决定直接回答、澄清还是请求切屏。
- 继续削减识屏链路中的风格模板约束，保留事实与安全边界，减少机械复述和固定腔调。

## 2.9.7 (2026-04-09)

### 优化

- 优化自然语言识屏的拒绝日志时机：现在只会在真正命中识屏求助时才记录权限拒绝，避免普通消息也反复刷出无关日志。
- 优化识屏回复风格与提示词约束，让回答更聚焦当前界面的确定信息，减少空泛总结和模板化收尾。

## 2.9.6 (2026-04-07)

### 优化

- 调整图片识别优先级：即使未开启 `use_external_vision`，只要 AstrBot 已配置图片转述 provider，识屏也会优先尝试该模型，再决定是否回退到当前聊天模型直连多模态。

### 修复

- 修复 AstrBot 图片转述链路仍被 `use_external_vision` 开关一并拦住，导致关闭外部视觉 API 后无法优先使用图片转述模型的问题。
- 修复部分 provider 不接受 `messages[].content[].image_url` 载荷时，插件未能自动切换到 `image_urls` 参数重试、从而直接返回 400 的兼容性问题。

## 2.9.5 (2026-04-06)

### 新增

- 新增更明确的内置识屏技能调用面，统一自然语言识屏的提示词拼装、隐式触发许可与执行入口，后续继续接 AstrBot 正式工具注册会更顺。
- 新增窗口标题应用描述映射模块，用于统一提取应用名、标题、场景和活动描述，帮助识屏回复更自然、更稳定。

### 优化

- 留空 `system_prompt` 或 `companion_prompt` 时，会更稳定地继承 AstrBot 当前人格，并把历史占位文案视为未填写。
- 自然语言识屏在群聊中的隐式触发更谨慎，通常需要更像是在明确叫到 Bot，减少普通聊天误触。
- WebUI 与配置说明同步改成“内置识屏技能”表述，配置入口更容易理解。

### 修复

- 修复陪伴模式说明容易让人误以为等于“开始自动观察”的文案问题。
- 修复自然语言识屏中管理员权限覆盖不完整的问题，现在会先过权限校验，再进入触发判断。
- 修复网页 / 报错类求助容易被应用启动器冲突规则误拦截的问题。
- 修复留空人格配置时，未必能正确沿用当前 AstrBot 人格的问题。
- 修复 WebUI 在插件重载时没有走到正确生命周期清理、以及同端口重启仍按“先起后停”导致旧端口残留占用的问题。

## 2.9.4 (2026-04-06)

### 修复

- 修复历史配置中异常 Windows 盘符路径格式可能导致插件加载失败的问题，例如 `:E:\...`、`/E:/...` 这类脏值现在会自动纠正后再使用。
- 清理发布包内不应携带的本地缓存与运行数据目录，降低跨机器安装时被旧环境残留影响的概率。

## 2.9.3 (2026-04-02)

### 新增

- WebUI 配置中心新增“基础设置 / 高级设置”切换，并提供游戏陪伴、工作陪伴、低打扰、轻量记录四个常用场景模板，减少首次配置时需要手动理解的大量参数。

### 优化

- 为窗口自动陪伴补上可配置的“窗口重连宽限期”，默认延长到 300 秒，更适合游戏结算、重开和重新匹配这类会短暂销毁窗口的场景。
- 窗口陪伴命中同一规则的新窗口时会优先续接当前会话，减少“结束提醒”和“开始提醒”在短时间内来回触发。
- 窗口陪伴会显式继承当前生效的自定义触发间隔和触发概率，WebUI 也会展示这组实际生效参数，避免和“窗口检查间隔”混淆。
- 设置页按功能开关动态隐藏子项，例如关闭窗口陪伴、录屏识屏、本地输入统计或日记后，不再默认展示对应的细分配置。

## 2.9.2 (2026-04-02)

### 优化

- 收紧自然语言识屏的触发条件，减少“帮我”类普通聊天被误判为识屏请求的情况。
- 统一管理员权限判断逻辑，让命令、自然语言识屏和默认主动消息目标使用同一套管理员来源。

### 修复

- 修复开启自然语言识别时容易误触发识屏的问题。
- 修复群聊中非管理员也可能通过自然语言触发识屏的问题。
- 修复自然语言识屏冷却未正确写回，导致短时间内可能重复触发的问题。

## 2.9.1 (2026-03-31)

### 新增

- 新增 `/kpi jk`，可查看今日本地输入统计，并在开启 LLM 回复时交由模型进一步整理。
- WebUI 的日记总览支持单独删除和批量删除。
- 今日活动 / 观察中的窗口标签支持一键加入窗口陪伴。

### 优化

- 对部分文本进行了优化。

### 修复

- 修复 `/kpi d` 返回日记图片时可能错误显示“今日观察”而不是“今日感想”的问题。
- 修复麦克风音量事件在持续高于阈值时可能频繁触发的问题。

## 2.9.0 (2026-03-29)

### 新增

- 新增本地输入统计，可记录键盘、点击、滚轮和鼠标移动，用来补充更像 KeyStats 的轻量工作回顾。
- 新增工作脉搏、今日工作轨迹、应用 / 网站 / 页面级活动轨迹，以及更完整的活动统计页结构。
- 新增离开自动挂起、回来自动恢复和长时间离开轻量提醒，减少非观影场景下的无效打扰。
- 新增活动识别自定义规则，支持按行配置应用 / 站点别名，让轨迹更贴合个人工作流。
- 新增日记窗口轨迹补料，识屏素材不足时可自动从当天窗口 / 应用 / 站点轨迹中抽取代表性活动补进日记。
- 新增独立活动轨迹采集开关。开启后，即使没有启动自动观察，也会按固定间隔记录当前活动窗口，并尽量拆出应用 / 网站 / 页面轨迹。

### 优化

- 开始 / 结束文案使用 LLM 生成时，结束自动观察的回复会参考最近识屏结果与插件回复，让收尾更贴近当前场景。
- 活动回顾统一接入有效工作口径，启用本地输入统计后会尽量扣除长时间无键鼠输入的空闲。
- 浏览器窗口标题识别进一步细化，可更稳定地拆出应用、站点和页面标题。
- 活动页支持窗口标题脱敏，更适合长期挂着使用。
- WebUI 保存体验升级：配置中心和运行设置会在保存失败时优先区分“真正失败”和“WebUI 正在重连”，并在可能恢复后自动拉取最新状态。
- 活动页补充关键词 / 类型 / 来源筛选、统计说明和清空筛选入口，工作轨迹回顾更容易按场景聚焦。
- WebUI 服务自检补充详细检查项与建议卡片，能更直观看到静态资源、目录、输入统计、独立轨迹采集和窗口读取能力是否正常。
- 日记生成改为更稳的补写链路，待写素材会落盘保存，低观察量场景下也能生成简版日记。
- 将 `pynput` 并入基础依赖，默认安装即可使用本地输入统计，不再需要额外安装输入统计依赖文件。
- 独立轨迹采集会在自动观察或窗口自动陪伴真正运行时自动让位，继续优先使用识屏链路写入的活动轨迹，减少两套来源互相打架。

### 修复

- 修复 Linux / Docker 场景下 `pyaudio / portaudio.h` 导致插件整体安装失败的问题，并将麦克风监听依赖拆分为可选安装项。
- 修复 Windows 初装后 WebUI 运行状态接口的 `name 'shutil' is not defined` 异常，避免单个字段报错影响整页接口。
- 修复 WebUI session 清理日志乱码、旧活动时间戳展示异常、Safari 浏览器识别过宽等问题。
- 修复日记仅在精确分钟触发、插件重载后待写条目丢失、手动补写和切换目录状态不同步等问题。

## 2.8.1 (2026-03-29)

### 优化

- 当开始/结束文案使用 LLM 生成时，结束自动观察的回复会参考最近几条识屏结果与插件回复，让收尾语更贴近刚刚的场景。

### 修复

- 修复 Linux / Docker 场景通过 AstrBot 上传安装插件时，`pyaudio` 因缺少 `portaudio.h` 导致整个插件安装失败的问题。
- 将麦克风监听依赖拆分为可选安装项，避免未启用的可选功能阻塞基础截图、录屏与 WebUI 能力。
- 修复环境检查中的依赖缺失提示乱码，并补充 Linux / macOS 下 `PyAudio` 的安装提示。
- 修复 Windows 初装后访问 WebUI 运行状态接口时报 `name 'shutil' is not defined`，并避免单个运行状态字段异常导致整页接口失败。

## 2.8.0 (2026-03-22)

### 新增

- 新增 `/kpi help`，可直接查看常用命令和最短上手路径。
- 新增学习管理命令：`/kpi learning`、`/kpi learned`、`/kpi unlearn`，支持查看开关、最近学习记录和回滚误学内容。
- 新增共同体验后的轻量追问能力：看片后可稳定追问喜好，游戏和测试场景改为小概率触发。

### 优化

- 主文件完成拆分，核心逻辑按运行时、主动陪伴、媒体分析、记忆与命令支持拆入 `core/` 模块，在保留原有命令入口的前提下提升可维护性。
- 学习链路升级为同时支持手动纠正、自然反馈、共同体验偏好学习，并补充学习原因记录、防误触、限频和话题绑定。
- 新增模式感知：看片时更偏陪伴，编程/办公时更偏助手，深度专注场景会进一步收敛主动打断。
- 补充任务收尾感提示，在工作场景明显告一段落时更容易给出顺势的下一步引导。
- 长期记忆补充第二版衰减与合并，支持低价值偏好过滤、近义偏好归并、陈旧低频记忆降权。
- 日记提示词与后处理同步收紧，减少“工作复盘腔”“命令腔”和过度夸张的旁白式表达，让今日日记更像贴身陪伴者写下来的私人记录。
- 日记排版调整为“正文 - 概览 - 观察”，WebUI 中将日记正文、结构化概览和观察时间线拆开展示；新日记正文也改为天气开头、日期结尾，减少重复信息块。
- `README`、状态命令和空状态提示同步更新，日常使用和排障路径更清晰。

### 修复

- 修复拆分后子模块错误推断插件数据目录导致最新截图保存失败的问题。
- 修复音量触发消息前缀 `【声音提醒】` 破坏沉浸感的问题。
- 清理多处全局无引用的遗留死代码，减少历史逻辑干扰后续维护。

## 2.7.3 (2026-03-21)

### 修复

- 修复 WebUI 中的多处乱码。
- 修复了补写日记功能的错误。

### 优化

- 在不使用外部 API 时，添加触发原因提示词注入以弥补信息缺失，并根据触发原因动态微调场景识别提示词，提供更有针对性的回复指导。
- 进行了音量触发的测试，确认功能正常。

### 调整

- 删除重复文件
- 现在可以在WEBUI中配置监控任务（音量/内存占用/电池电量）的触发阈值了。

## 2.7.2 (2026-03-20)

### 调整

- 更新插件版本号到 `2.7.2`。
- 新增敏感场景自动沉默：检测到支付页、密码管理器、登录/验证码相关界面、聊天窗口时，本轮只记录"检测到敏感界面，已跳过回复"，不再主动说话。
- 主动陪伴补充"沉默也是互动"规则，在变化不大或用户刚被打断等场景下，允许本轮只更新内部观察而不发言。
- 开始/结束文案改为会参考最近上下文，不再只走固定模板。
- `/kp`、`/kpr`、`/kps` 指令权限调整为管理员权限，不再允许所有人使用。
- 麦克风监听支持运行时动态启停：打开 `enable_mic_monitor` 无需重启插件即可生效，音量采样采用更稳定的实现。

### 优化

- 新增情绪记忆短缓存，能在"刚被打断 / 刚卡关 / 刚完成"后的几轮回复里延续前一段情绪，不再每次都像重新开场。
- 回复生成补充开场轮换约束，降低"我看了下""你现在像是在""可以试试"这类固定句式的连续复用。
- 主动陪伴链路进一步增强连续性和低打扰表现，让回复更像在旁边跟着当前节奏，而不是每轮都重新起势。
- 麦克风音量采样优化：优先查找默认输入设备，读取多段样本，避免 int16 平方溢出导致音量异常。

## 2.7.1 (2026-03-19)

### 调整

- 日记补强兜底逻辑，避免只剩标题和天气；日记页也不再重复渲染“今日概览”。
- 保留的观察记录从最多9条增加到最多50条，以覆盖更多场景。
- 休息提醒改为更严格的“推测作息 + 每天仅一次”，并弱化深夜时间提示词对连续催睡的诱导。
- 主动回复补充更强的连续性约束，减少重复称呼、重复开场和连续几条都重新起势的问题，现在bot的回复应该更加拟人自然了。

### 优化

- 主动陪伴会结合与上一条主动回复的时间间隔，区分“顺着接话”“自然续上”和“隔了一阵子后重新靠近”的语气。
- 建议型回复继续保持主导，但允许偶尔只做轻观察、共鸣或陪伴，降低机械感。
- Gemini 原生视频链路会尽量保留完整录屏以保住音轨，并明确要求模型只在能辨识音频时再参考音频。
- 录屏模式补充当前截图锚点，减轻“像在回复上一轮画面”的延后感。
- 自动化后台任务在录屏模式下改为每次触发现录一整段，再结合当前截图分析，更贴近触发当下。

## 2.7.0 (2026-03-17)

### 调整

- 重写说明文档，整理安装、配置、排障和 WebUI 使用说明。
- 精简重复指令，对外只保留简化版入口：
  - `/kpi status`
  - `/kpi p`
  - `/kpi d`
  - `/kpi cd`

### 优化

- 录屏识屏新增轻量采样与自适应采样策略，优先抽关键帧后再决定是否进入完整视频分析。
- 日记升级为“双层产物”，除自然语言正文外还会生成结构化摘要。
- 主动消息补充更细的打扰控制，包括相似回复冷却、同窗口频率限制和手动发言后暂缓打断。
- WebUI 支持查看更可解释的识屏信息，包括触发原因、素材类型、识别摘要与最终回复。
- `ffmpeg.exe` 默认改为持久化到插件数据目录的 `bin` 文件夹，避免插件更新后重复放置。
- 休息提醒改为优先根据最近几天最后一次窗口活动推测作息，并限制为每天仅提醒一次；推测出的作息规律会写入长期记忆。
- 长期记忆与活动统计继续补强，提升持久化、检索质量和可观察性。

## 2.6.2 (2026-03-17)

### 调整

- 修正午夜自动日记的日期归属。
- `/kpi cd` 在凌晨两点前默认补写前一天的日记。
- 优化 `/kpr` 的过程反馈与录屏链路超时策略，降低录屏模式下的超时概率。

## 2.6.1 (2026-03-16)

### 新增

- 新增 `/kpi ffmpeg [路径]` 指令，可自动复制 `ffmpeg.exe` 到插件目录。
- 新增录屏模式配置校验，避免检查间隔小于录屏时长。

### 调整

- `/kpr` 现在始终执行一次录屏识别，不再受全局识别素材开关影响。
- 优化观察记录去重逻辑，保留更多有效观察。
- `recording_fps` 支持小数。

## 2.6.0 (2026-03-15)

### 新增

- 新增截图/录屏识别素材开关。
- 新增录屏识别链路与 `/kpr` 指令。
- 新增 Gemini 原生多模态直连优化。
- 新增 `ffmpeg_path`、`recording_fps` 和 `recording_duration_seconds` 配置项。
- WebUI 增加最新素材预览。

### 调整

- `use_external_vision` 明确区分“外部视觉 API 两步识别”和“直接发送给 AstrBot 的多模态识别”。
- `/kp` 固定为截图识别，`/kpr` 固定为录屏识别。
- 录屏默认参数调整为 `1 fps / 10 秒`。
